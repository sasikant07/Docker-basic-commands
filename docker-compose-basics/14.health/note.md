# Healthcheck

## build the docker
`docker compose build`

## create compose
`docker compose create`

## start compose
`docker compose start`

## check container
`docker container ps`

## stop docker compose
`docker compose stop`
